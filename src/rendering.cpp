#include "rendering.h"
#include <algorithm>
//#include <SDL.h>



bool RenderScalarField(const std::vector<std::vector<float>>& field)
{
    const int width = field[0].size();
    const int height = field.size();

    //if (SDL_Init(SDL_INIT_VIDEO) != 0) { return false; }

    return false;
}
